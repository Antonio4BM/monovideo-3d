services:
  monovideo3d:
    build: .
    volumes:
      - ./data:/monovideo/data
    working_dir: /monovideo
    ports:
      - 8000:8000
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]